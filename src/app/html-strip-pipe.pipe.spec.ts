import { HtmlStripPipePipe } from './html-strip-pipe.pipe';

describe('HtmlStripPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlStripPipePipe();
    expect(pipe).toBeTruthy();
  });
});
